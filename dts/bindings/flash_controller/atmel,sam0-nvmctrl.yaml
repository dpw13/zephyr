description: Atmel SAM0 NVMC (Non-Volatile Memory Controller)

compatible: "atmel,sam0-nvmctrl"

include:
  - name: flash-controller.yaml
  - name: atmel,assigned-clocks.yaml

properties:
  lock-regions:
    type: int
    required: true
    description: Number of lock regions
