description: Atmel SAM0 watchdog

compatible: "atmel,sam0-watchdog"

include:
  - name: base.yaml
  - name: atmel,assigned-clocks.yaml

properties:
  reg:
    required: true

  interrupts:
    required: true
